# HW 7 (threads and synchronization)

Program:

- `thread_sum.c`

Build:

```sh
cmake -S .. -B ../build
cmake --build ../build --target dz7_thread_sum
```

Run:

```sh
../build/dz7/dz7_thread_sum
```
