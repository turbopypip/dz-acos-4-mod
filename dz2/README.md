# HW 2 (System calls)

Build from the repository root:

```bash
cmake -S . -B build
cmake --build build
```

Programs:

- `dz2_print_lines <file> <start-line> <line-count>`
- `dz2_middle_line <file>`

Both programs use Unix system calls for file I/O.

